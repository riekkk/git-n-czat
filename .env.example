VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Required for thermal receipt printing — PrintNode API key (printjobs
# are sent to printerId 75810640, set in src/lib/printer.ts).
VITE_PRINTNODE_API_KEY=
